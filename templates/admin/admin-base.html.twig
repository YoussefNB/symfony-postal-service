{% extends 'base.html.twig' %}
{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark elegant-color">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{path('admin')}}">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('courrier-my-packages')}}">My packages</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('courrier-free-packages')}}">Free packages</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('security_logout')}}">Log out</a>
                </li>
            </ul>
        </div>
    </nav>
    {% block content %}{% endblock %}
{% endblock %}